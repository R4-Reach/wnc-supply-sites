plugins {
	// Lets Gradle auto-provision (download) JDKs for toolchains and the daemon JVM.
	id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'supplies-database'
